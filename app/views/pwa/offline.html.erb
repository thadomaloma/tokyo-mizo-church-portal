<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>You're Offline · TMC Portal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
    <meta name="theme-color" content="#0b1f33">
    <style>
      * { box-sizing: border-box; }

      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.5rem;
        padding-top: max(1.5rem, env(safe-area-inset-top));
        padding-bottom: max(1.5rem, env(safe-area-inset-bottom));
        background: radial-gradient(circle at 82% 0%, rgba(14, 116, 144, 0.12), transparent 28rem), #f4f7fa;
        font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        color: #0b1f33;
      }

      .card {
        width: 100%;
        max-width: 26rem;
        background: #ffffff;
        border: 1px solid rgba(226, 232, 240, 0.9);
        border-radius: 1.75rem;
        box-shadow: 0 24px 70px rgba(15, 23, 42, 0.12);
        padding: 2.25rem 1.75rem;
        text-align: center;
      }

      .icon {
        width: 56px;
        height: 56px;
        border-radius: 16px;
        margin: 0 auto 1.25rem;
        overflow: hidden;
        border: 1px solid #e2e8f0;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #ffffff;
      }

      .icon img {
        width: 40px;
        height: 40px;
        object-fit: contain;
      }

      h1 {
        margin: 0 0 0.6rem;
        font-size: 1.375rem;
        font-weight: 900;
        letter-spacing: -0.01em;
      }

      p {
        margin: 0;
        font-size: 0.9rem;
        line-height: 1.5;
        color: #64748b;
        font-weight: 600;
      }

      .actions {
        margin-top: 1.75rem;
        display: flex;
        flex-direction: column;
        gap: 0.6rem;
      }

      button, a.secondary {
        min-height: 44px;
        border-radius: 12px;
        font-size: 0.875rem;
        font-weight: 900;
        border: none;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        text-decoration: none;
      }

      .retry {
        background: #0b1f33;
        color: #ffffff;
      }

      .retry:hover {
        background: #123c57;
      }

      .status {
        margin-top: 1.25rem;
        font-size: 0.75rem;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: #94a3b8;
      }

      .status[data-online="true"] {
        color: #047857;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <div class="icon">
        <img src="/icon-192.png" alt="Tokyo Mizo Church Portal">
      </div>

      <h1>You're Offline</h1>
      <p>TMC Portal data thar ber en turin internet connection a ngai.</p>

      <div class="actions">
        <button type="button" class="retry" onclick="location.reload()">Retry</button>
      </div>

      <p class="status" data-status>Internet connection nghah mek&hellip;</p>
    </div>

    <script>
      (function () {
        var status = document.querySelector("[data-status]");

        function render() {
          var online = navigator.onLine;
          status.textContent = online ? "Internet a awm leh ta" : "Internet connection nghah mek…";
          status.setAttribute("data-online", String(online));
        }

        window.addEventListener("online", function () {
          render();
          location.reload();
        });
        window.addEventListener("offline", render);
        render();
      })();
    </script>
  </body>
</html>
